import React from 'react';
import SmartConnectionStart from '../connection/InlineConnectionSetup';

/**
 * Connection setup screen when not connected to any database
 */
const ConnectionSetupScreen = () => {
  return <SmartConnectionStart />;
};

export default ConnectionSetupScreen;
